<div class="container my-5">
  <h1>Your Astrological Profile</h1>

  <div class="card shadow">
    <div class="card-body p-4">

      <div class="mb-4">
        <h3>User Information</h3>
        <p><strong>Email:</strong> <%= current_user.email %></p>

        <% if current_user.first_name.present? %>
          <p><strong>Name:</strong> <%= current_user.first_name %> <%= current_user.last_name %></p>
        <% end %>
      </div>

      <hr>

      <div class="mb-4">
        <h3>Birth Information</h3>
        <p><strong>📅 Birth Date & Time:</strong> <%= @profile.birth_datetime.strftime("%B %d, %Y at %I:%M %p") %></p>
        <p><strong>📍 Birth City:</strong> <%= @profile.birth_city %></p>
        <p><strong>🌍 Birth Country:</strong> <%= @profile.birth_country %></p>
      </div>


      <div class="d-grid gap-2 mt-4">
        <%= link_to "🌟 View your future",services_path , class: "btn btn-primary " %>
      </div>

    </div>
  </div>
</div>
